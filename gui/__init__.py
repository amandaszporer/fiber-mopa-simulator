"""Streamlit GUI package for the Yb-Doped Fiber MOPA Simulator.

A thin front-end over the existing engine (`components.py`, `framework.py`,
`utils/plotting.py`). No engine code is modified by anything in this package.
Launch with `streamlit run app.py` from the project root.
"""
